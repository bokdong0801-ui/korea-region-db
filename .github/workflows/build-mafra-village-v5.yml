name: Build MAFRA Rural Village V5 Overlay

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/build-mafra-village-v5.yml'
      - 'scripts/download_mafra_villages.py'
      - 'scripts/import_mafra_villages.py'
      - 'scripts/augment_geo_v5_villages.py'

jobs:
  build-mafra-village-v5:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - run: mkdir -p /tmp/v4 /tmp/mafra/raw /tmp/mafra/normalized /tmp/v5/master
      - name: Download audited V4 artifact
        env:
          GITHUB_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          curl -fL -H "Authorization: Bearer $GITHUB_TOKEN" -H 'Accept: application/vnd.github+json' -H 'X-GitHub-Api-Version: 2022-11-28' \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/artifacts/10355006058/zip" -o /tmp/v4.zip
          unzip -oq /tmp/v4.zip -d /tmp/v4
          test -s /tmp/v4/v4/master/places_geo_master.csv
      - name: Download official MAFRA nationwide rural village CSV
        run: python scripts/download_mafra_villages.py --snapshot-date 2026-09-15 --out-dir /tmp/mafra/raw
      - name: Normalize and map village IDs to legal regions
        shell: bash
        run: |
          RAW=$(find /tmp/mafra/raw -name 'mafra_rural_villages_*.csv' | head -n1)
          test -n "$RAW"
          python scripts/import_mafra_villages.py "$RAW" \
            --v4-places /tmp/v4/v4/master/places_geo_master.csv \
            --snapshot-date 2026-09-15 --out-dir /tmp/mafra/normalized
          cat /tmp/mafra/normalized/village_import_report.json
          echo '=== UNRESOLVED REGIONS ==='
          cat /tmp/mafra/normalized/village_unresolved_regions.csv
          echo '=== BAD IDS ==='
          cat /tmp/mafra/normalized/village_bad_ids.csv
      - name: Exact 3306 standard plus 15 official scope IDs CI gate
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          r=json.load(open('/tmp/mafra/normalized/village_import_report.json',encoding='utf-8'))
          standard=r['id_resolution_counts'].get('FIRST_10_DIGITS',0)
          scope=r['id_resolution_counts'].get('EIGHT_DIGIT_SCOPE_PLUS_00',0)
          assert standard == 3306, r
          assert scope == 15, r
          assert r['unique_village_ids'] == 3321, r
          assert r['places'] == 3321, r
          assert r['relations'] == 3321, r
          assert r['unresolved_regions'] == 0, r
          assert r['bad_ids'] == 0, r
          assert r['relations'] + r['unresolved_regions'] == r['places'], r
          report={
              'standard_ids_expected':3306,
              'standard_ids_resolved':standard,
              'official_8digit_scope_expected':15,
              'official_8digit_scope_resolved':scope,
              'total_expected':3321,
              'total_resolved':r['relations'],
              'unresolved_regions':r['unresolved_regions'],
              'bad_ids':r['bad_ids'],
              'status':'PASS'
          }
          Path('/tmp/mafra/normalized/village_exact_ci_report.json').write_text(json.dumps(report,ensure_ascii=False,indent=2),encoding='utf-8')
          print('MAFRA EXACT CI PASS',json.dumps(report,ensure_ascii=False))
          PY
      - name: Build V5 village overlay only after exact gate
        shell: bash
        run: |
          python scripts/augment_geo_v5_villages.py \
            --v4-dir /tmp/v4/v4/master \
            --village-dir /tmp/mafra/normalized \
            --out-dir /tmp/v5/master
      - name: Verify V5 master includes every official MAFRA record
        shell: bash
        run: |
          python - <<'PY'
          import csv, json
          from pathlib import Path
          p=Path('/tmp/v5/master/places_geo_master.csv')
          assert p.exists() and p.stat().st_size > 0, p
          with p.open(encoding='utf-8-sig', newline='') as f:
              rows=list(csv.DictReader(f))
          villages=[r for r in rows if r.get('source_id') == 'mafra_rural_village_basic' and r.get('place_type') in {'RURAL_VILLAGE','RURAL_CENTER'}]
          assert len(villages) == 3321, len(villages)
          report={
              'standard_village_ids':3306,
              'official_8digit_scope_ids':15,
              'v5_mafra_entities':len(villages),
              'v5_total_places':len(rows),
              'status':'PASS'
          }
          Path('/tmp/v5/v5_mafra_ci_report.json').write_text(json.dumps(report,ensure_ascii=False,indent=2),encoding='utf-8')
          print(json.dumps(report,ensure_ascii=False))
          PY
      - name: Upload MAFRA village audit artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: korea-mafra-village-v5-audit-20260915
          path: |
            /tmp/mafra/**
            /tmp/v5/**
          if-no-files-found: warn
          retention-days: 90
